# Flux image automation for homeranger-api + homeranger-web.
#
# Each ImageRepository scans GHCR for tags; each ImagePolicy selects the highest
# semver >= 0.0.1; the single ImageUpdateAutomation rewrites the $imagepolicy
# markers in ./infra/deploy/base and commits the bump.
#
# push.branch = main (single-user repo → direct push; no PR-gate side-branch).
# Doxus uses a side-branch + auto-PR for its multi-author flow; homeranger pushes
# straight to main because there is one operator and no validate-infra gate yet.
#
# The image-reflector + image-automation controllers live in flux-system, so
# these resources are namespaced there. The GHCR pull secret for the scanner is
# ghcr-pull-secret-flux (see ghcr-secret.example.yaml).
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: homeranger-api
  namespace: flux-system
spec:
  image: ghcr.io/lynxsyn/homeranger-api
  interval: 5m
  secretRef:
    name: ghcr-pull-secret-flux
---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: homeranger-web
  namespace: flux-system
spec:
  image: ghcr.io/lynxsyn/homeranger-web
  interval: 5m
  secretRef:
    name: ghcr-pull-secret-flux
---
# M4 — the BullMQ worker image joins the scan set.
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: homeranger-processor
  namespace: flux-system
spec:
  image: ghcr.io/lynxsyn/homeranger-processor
  interval: 5m
  secretRef:
    name: ghcr-pull-secret-flux
---
# M6 — the leader-locked scheduler image joins the scan set.
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: homeranger-scheduler
  namespace: flux-system
spec:
  image: ghcr.io/lynxsyn/homeranger-scheduler
  interval: 5m
  secretRef:
    name: ghcr-pull-secret-flux
---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: homeranger-api
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: homeranger-api
  filterTags:
    pattern: '^v?(?P<version>[0-9]+\.[0-9]+\.[0-9]+)$'
    extract: '$version'
  policy:
    semver:
      range: '>=0.0.1'
---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: homeranger-web
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: homeranger-web
  filterTags:
    pattern: '^v?(?P<version>[0-9]+\.[0-9]+\.[0-9]+)$'
    extract: '$version'
  policy:
    semver:
      range: '>=0.0.1'
---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: homeranger-processor
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: homeranger-processor
  filterTags:
    pattern: '^v?(?P<version>[0-9]+\.[0-9]+\.[0-9]+)$'
    extract: '$version'
  policy:
    semver:
      range: '>=0.0.1'
---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: homeranger-scheduler
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: homeranger-scheduler
  filterTags:
    pattern: '^v?(?P<version>[0-9]+\.[0-9]+\.[0-9]+)$'
    extract: '$version'
  policy:
    semver:
      range: '>=0.0.1'
---
apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImageUpdateAutomation
metadata:
  name: homeranger
  namespace: flux-system
spec:
  interval: 5m
  sourceRef:
    kind: GitRepository
    name: homeranger
  git:
    checkout:
      ref:
        branch: main
    commit:
      author:
        name: fluxcdbot
        email: fluxcdbot@users.noreply.github.com
      messageTemplate: |
        chore(flux): update image tags

        {{ range .Updated.Images }}- {{ . }}
        {{ end }}
    push:
      branch: main
  update:
    path: ./infra/deploy/base
    strategy: Setters
